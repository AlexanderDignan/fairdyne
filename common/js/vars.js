const SHIELD_DISTANCE = 32;
const HEART_SIZE = 16;

const BOX_ADJUST_SPEED = 0.5; // px/ms
const MOVEMENT_SPEED = 0.16;  // px/ms
const SPEAR_SPEED = 0.25;

const BEAT    = 60 / 148;      // Death by Glamour: ~0.405s per beat (seconds)
const BEAT_MS = BEAT * 1000;   // same beat duration in milliseconds
